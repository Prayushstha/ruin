# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files may contain secrets (Supabase keys etc.) — never commit them.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntws*
*.njsproj
*.sln
*.sw?

# Local notes / drafts are NOT ignored by default. If you want to keep a
# specific scratch file out of git, add it explicitly.
